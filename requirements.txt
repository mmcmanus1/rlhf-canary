# Core dependencies
transformers>=4.35.0
datasets>=2.14.0
trl>=0.12.0,<0.15.0  # Pin to avoid dtype bug in TRL 0.26+
peft>=0.6.0
torch>=2.0.0
pydantic>=2.0.0
click>=8.0.0
PyYAML>=6.0
accelerate>=0.24.0

# Optional: Quantization support
bitsandbytes>=0.41.0

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
ruff>=0.1.0
